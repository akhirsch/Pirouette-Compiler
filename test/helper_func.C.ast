(
  (compute_local x) := ((()), (()))
  (main) :=
    (let
       (
         (_) := ((()) (()))
         (_) := ((()) (()))
         ((x, y)) := ((()) (ret (2)))
       )
     in
       (let
          (
            (_) := (())
            (_) := ((ret ((x, y))) ~> A)
          )
        in
          (let (
                 (_) := (())
               ) in (()))))
)
