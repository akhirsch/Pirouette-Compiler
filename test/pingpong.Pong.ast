(
  (helper
    curr) :=
    (let
       (
         (x) := (<~ Ping)
       )
     in
       (let
          (
            (y) := (ret (x - 1))
          )
        in
          (if
             (ret (x > 0))
           then
             (choose L for Ping;
              let (
                    (_) := ((ret (y)) ~> Ping)
                  ) in ((()) (())))
           else
             (choose R for Ping;
              let (
                    (_) := ((ret (y)) ~> Ping)
                  ) in ((()) (()))))))
  (main) := (let (
                   (_) := (())
                 ) in ((()) (())))
)
