(
  (compute_local x) := ((()), (()))
  (main) :=
    (let
       (
         (_) := ((()) (()))
         ((x, y)) := ((()) (ret (1)))
         (_) := ((()) (()))
       )
     in
       (let
          (
            (_) := ((ret ((x, y))) ~> A)
            (_) := (())
          )
        in
          (let (
                 (_) := (())
               ) in (()))))
)
