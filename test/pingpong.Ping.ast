(
  (helper
    curr) :=
    (let
       (
         (_) := ((ret (curr)) ~> Pong)
       )
     in
       (let
          (
            (_) := (())
          )
        in
          (allow Pong choice
           | L -> let (
                        (x) := (<~ Pong)
                      ) in ((ret (helper)) (ret (x)))
           | R ->
               let
                 (
                   (x) := (<~ Pong)
                 )
               in
                 ((ret (print_endline)) (ret ("done"))))))
  (main) := (let (
                   (x) := (ret (100))
                 ) in ((()) (ret (x))))
)
