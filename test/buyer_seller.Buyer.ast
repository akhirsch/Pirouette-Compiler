(
  (main) :=
    (let
       (
         (_) := (())
       )
     in
       (let
          (
            (bid) := (ret (5))
          )
        in
          (let
             (
               (_) := ((ret (bid)) ~> Seller)
             )
           in
             (allow Seller choice
              | L -> ret (false)
              | R -> ret (true)))))
)
