(
  (compute_local x) := ((()), (()))
  (main) :=
    (let
       (
         ((x, y)) := ((()) (ret (0)))
         (_) := ((()) (()))
         (_) := ((()) (()))
       )
     in
       (let
          (
            ((x1, y1)) := (<~ B)
            ((x2, y2)) := (<~ C)
          )
        in
          (let (
                 ((sx, sy)) := (ret ((x + x1 + x2, y + y1 + y2)))
               ) in (()))))
)
